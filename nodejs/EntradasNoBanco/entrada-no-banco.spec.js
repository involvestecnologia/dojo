const { assert } = require('chai');
const entrada = require('./entrada-no-banco');

suite('Valida entradas de hora no banco', () => {
  describe('Valida se entrada esta em horário comercial', () => {
    it('para uma entrada', () => {
      assert.equal(este, aquele);
    });
  });
});
